{# app/Resources/views/default/new.html.twig #}

{% if (errors) %}
    <h3>The author has the following errors</h3>
    <ul>
    {% for error in errors %}
        <li>{{ error.message }}</li>
    {% endfor %}
    </ul>
{% endif%}

{{ form_start(form) }}
{{ form_widget(form) }}
{{ form_end(form) }}